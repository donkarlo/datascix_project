class Svm:
    def __init__(self, kernel:Any):
        """
        if teh pair_set are non linearly being separated then kernel sends them to a higher space so that there the separation is possible by a line
        Args:
            kernel:
        """
        pass